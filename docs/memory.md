# Память проекта (между чатами Cursor)

Краткие факты и решения. **Обновляй после важных договорённостей в чате.**

---

## ▶ СЕЙЧАС В РАБОТЕ (читать первым)

**Дата:** 2026-06-06  

**Цепочка проверки:** только `check_agent` → `inject_agent`. **Агента перепроверки (verify) не будет** — не планировать, не восстанавливать из git.

**Сделано (2026-06-05, UI):** убран `build {{ git_head }}` из шапки (остался только в `?v=` для cache-bust); фавикон — синий шестиугольник `webapp/static/favicon.svg` (`#1a56a0`).

**Сделано (сайдбар):** дата + руководитель в `#dateBlock` без подписей над полями; руководитель — `#leaderSelect` (Аниськов В.И. / Манджиев И.А.), стиль как у даты, стрелка справа; смена подписей при **«Подготовить»** (`leader_switch.py`, SSE). В docx для Манджиева по-прежнему «И.О. …» — только подпись в UI без «(И.О.)».

**Сделано (2026-06-06):** одна зелёная кнопка **«Проверить и сформировать»** — `prepareReports()` сначала `runCheck()` (check→inject), затем руководитель → макросы → дата в болванках → сборка → переименование. Отдельной кнопки «Проверить и исправить» нет.

**Сделано (2026-06-05):** кнопки «Подготовить» и «Сформировать» объединены (предшественник текущей одной кнопки).

Новый чат — начинать с этого раздела; не переобъяснять руководителя/дату.

### Порядок в сайдбаре (сейчас)

```
загрузка → [дата] [руководитель] → Проверить и сформировать
→ Скачать (ZIP) → …
```

**Проверить и сформировать** = `prepareReports()`: `runCheck()` → `switchLeader` → macro prepare → дата в болванках → `mergeAll()` → rename.

| Кнопка | JS | Что делает |
|--------|-----|------------|
| Проверить и сформировать | `prepareReports()` | check→inject → switchLeader → macro prepare → дата в болванках → сборка → rename |
| Скачать (ZIP) | `downloadAll()` | два архива: `отчёты.zip` + `исправленные.zip` |

---

### Блок 1 — проверка *(check → inject, внутри «Проверить и сформировать»)*

| UI | Код / API |
|----|-----------|
| *(шаг 1 зелёной кнопки)* | `runCheck()` → `POST /check/descriptions/stream` |

**Текущая цепочка:** `check_agent` → `inject_agent` → **запись в загрузку**. Скачать `{имя}_исправлен.docx` — `/download/fixed/...`.

В логах сервера: **фаза 1** `[CHECK_AGENT]` по всем → **фаза 2** `[INJECT_AGENT]` по всем. UI: одна карточка «Проверка отчётов», раскрываемые логи «Отчёты по файлам» на `done`. После `git pull` — **перезапуск** bat и **Ctrl+F5**.

| Слой | Файл |
|------|------|
| UI | `webapp/templates/home.html`, `webapp/templates/daily.html`, `webapp/static/app.js` |
| API | `webapp/main.py` — stream check, `/download/fixed/...` |
| AI + inject | `check_agent.py`, `inject_agent.py` — **не откатывать** колонки Участок/ПК/Ссылка в check |
| Загрузка (сырые + после проверки) | temp `sk_reports_work/uploads/` — см. `UPLOAD_DIR` в `webapp/main.py` |

Важно: inject пишет в ячейку-заголовок **«Описание действий»**; после проверки перезаливать не нужно. Отдельных папок `output/` в репо нет.

---

### Руководитель в отчёте (готово, 2026-06-05)

| UI | Действие |
|----|----------|
| `#macroReportDate` + `#leaderSelect` в `#dateBlock` | без видимых `field-label`; `localStorage` для руководителя |
| `.field-box--leader` | видимое `#leaderDisplay` + прозрачный `select` поверх |

Смена подписей при **«Подготовить»** (первый шаг): для всех `.docx` в `UPLOAD_DIR` — rule-based **4 ячейки** (шапка/подвал). Без Ollama.

Координаты: поиск по тексту в правой зоне таблицы; сетки **GRID_COLS_6** и **GRID_COLS_7** (ghost-колонка) — калибровка на образцах в корне репо (`Ежедневный отчет (ЮНС)…`).

| Слой | Файл |
|------|------|
| UI | `#leaderSelect`, `prepareReports()` → `switchLeader()` (SSE) |
| API | `webapp/main.py` → `POST /switch-leader/stream/{leader}` |
| Логика | `sk_reporter/leader_switch.py` (+ `template_layout._main_table_indices`) |

**2026-06-05:** удалён `sk_reporter/agent/leader_ai_agent.py` (Ollama) — заменён `leader_switch.py`.

---

### Как работать агенту в новом чате

1. Прочитать **«СЕЙЧАС В РАБОТЕ»** целиком.
2. **Verify / перепроверка — не делать**, не восстанавливать из истории git.
3. Блок 1 стабилен: `check_agent` → `inject_agent`; не ломать UI логов и не трогать `check_agent` без необходимости.
4. Не уводить в VPS / 20 bat / таблицу-форму без запроса (фон — `PROJECT_CONTEXT.md`).
5. Коммит/push — только по явной просьбе.
6. После `git pull` на офисном ПК — перезапустить bat.

---

## Архитектура и пути

### 2026-06-04 — где лежат файлы
- **Сырые и исправленные** (рабочие): temp `…/sk_reports_work/uploads/` (`UPLOAD_DIR`)
- **Готовые сводные**: temp `…/sk_reports_work/results/`
- Папки `output/` / `sk_reporter/output/` в репо удалены (2026-06-04)
- После `git pull` на офисном ПК — **перезапустить** `launcher/SK-Reporter.bat`

### 2026-06-05 — сайдбар: дата + руководитель
- Кнопки Аниськов/Манджиев убраны; выбор в списке под датой; UI без лейблов над полями
- `prepareReports()` первым шагом вызывает `switchLeader()`
- Одна зелёная кнопка «Подготовить и сформировать» (2026-06-05)

### 2026-06-05 — блок 2 без AI-агента
- Удалён `sk_reporter/agent/leader_ai_agent.py`; смена руководителя только через `leader_switch.py`

### 2026-06-05 — verify удалён (окончательно)
- Удалены `verify_agent.py`, SSE-события verify, вторая карточка в UI; цепочка **check → inject**
- **2026-06-06:** агент перепроверки **не планируется** — не возвращать

### 2026-06-05 — мелочи UI
- Шапка без `build …`; фавикон `favicon.svg` (синий шестиугольник)

### 2026-06-04 — inject «Описание действий»
- Текст от check_agent вставляется в **ячейку-заголовок** «Описание действий»
- Скачать «как _исправлен» — панель «Исправленные» справа (`/download/fixed/{имя}_исправлен.docx`)

---

## Фон проекта (кратко)

- ~20 инженеров, ~40–45 ежедневных отчётов docx, 2–4 ч/день ручной правки у руководителя СК
- SK-Reporter — временное решение; в планах таблица объёмов → Word → почта → автосбор
- Детали — в `docs/PROJECT_CONTEXT.md`

---

## Заполни по ходу (владелец проекта)

### Организация и масштаб
<!-- уточнения -->

### Решения по развёртыванию
<!-- один ПК / VPS / bat -->

### Промпты и качество отчётов
<!-- check_agent -->

### Не делать / осторожно
- Не возвращать `verify_agent` / перепроверку после check
- <!-- не коммитить .env, не force push -->
