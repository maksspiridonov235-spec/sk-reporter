# Память проекта (между чатами Cursor)

Единый источник контекста между сессиями. **Читать перед нетривиальными задачами; обновлять после решений, шагов, блокеров.**

---

## Журнал сессий

### 2026-06-26 — UX: форма и меню
- **Docx:** multiselect как `/daily`.
- **Шаблон расстановки:** обязателен (файл на отправку каждый день).
- **Выход расстановки:** только `.xlsm`, не HTML.
- **Меню:** справочники — **Планирование**; генерация (дата, загрузка, ZIP) — **Отчётность**.

### 2026-06-26 — импорт «Ядро.xlsx»
- **Файл:** `Ядро.xlsx` — листы «Подрядчики» (16 строк) и «Объекты» (55 строк).
- **Код:** `sk_reporter/core_xlsx.py`, `core_import.py`, `scripts/import_core_xlsx.py`, `POST /api/planning/core/upload-xlsx`.
- **Подрядчики:** все колонки Excel → таблица `contractors` (+ новые поля: file_label, gen/sub, договор, контакты…).
- **Проекты:** stub — `id`=шифр (при дубле `{шифр}__{contractor_id}`), `title`=шифр, `object_name`=наименование; **не перезаписывает** карточки с `content` (ВОР/ТЛ).

### 2026-06-26 — UX расстановки
- **Фронт:** одна форма — дата + docx + Прил.7 + шаблон расстановки (всё через загрузку, как «пути» в референсе); одна кнопка → ZIP со всеми выходами этапов; справочники из PostgreSQL.

### 2026-06-26 — план «Расстановка / Прил.7»
- **Референс:** `Noviy proekt AI/` — только пример логики; папку удалить после реализации.
- **Решено:** черновик интеграции **откатан**; код пишем по плану после согласования открытых вопросов.
- **Решено:** должности + описания → PostgreSQL; подрядчики и проекты — импорт из Excel (файлы пришлёт пользователь); прод RelaxDev; один комплект xlsm; UI как `/daily` → готовые файлы; строгая сверка (детали — в плане); АОС Персонал **не в v1**; запуск только из UI.
- **План:** [`docs/planning_deployment.md`](planning_deployment.md).

### 2026-06-26 (продолжение)
- **Что сделано:** зачистка по фронту — удалены API без вызовов из UI (`/api/build`, `/diagnose/reports`, `/clear/results`, `/files/prescriptions`, seed ОТКК, назначение инженеров); dead functions в `project_db`, `contractor_db`, `tk_catalog`, `template_layout`; скрипты-дубликаты (`import_personnel_xlsx`, `seed_otkk*`, `purge_empty_otkk`, тесты TE/LLM/ND, `git-pull-office.ps1`); `data/tk/manifest.yaml`.

### 2026-06-26
- **Что сделано:** полное удаление ЛУВР (см. выше); зачистка dead code: `otkk_rich.py`, `otkk_verbatim.py`, `vor_legacy.py`, `vor_parser.py`, `techexpert_client.py`, `normative_web_fallback.py`, `te_env.py`, скрипты `build_otkk_data.py`, `test_techexpert.py`; парсинг B19 → `prescriptions/normative_parse.py`; `otkk_parser.py` — только `content_to_plain_text`.
- **Решение:** планирование и ВОР — только PostgreSQL; эталоны проектов/ОТКК (`*_data.py`, `project_data/*.json`) — seed в БД, не удалять.

### 2026-06-16
- **Что сделано:** зафиксирован протокол работы агента с памятью (4 раздела, журнал сверху).
- **Что сделано:** удалён модуль «ЛУВР» из UI/роутов (страницы `/planning/luvr` и `/api/luvr/*` больше нет).
- **Что сделано:** удалены все `.bat` (офисный `launcher/`, `engineer/launchers/`), автогенерация bat в `hub.py`; прод — только RelaxDev.
- **Что сделано:** вкладка **Проекты** — карточки в PostgreSQL (ВОР + ТЛ вручную из Word, UI «Открыть»); пилот `SUP-PDR-ENC-001-DD-ST01-EV_00.1`; импорт с диска удалён.
- **Что сделано:** справочник **Сотрудники** — только PostgreSQL на RelaxDev (`personnel`, импорт Excel, UI `/planning/personnel`); `personnel.yaml` и yaml-fallback удалены.
- **Что решили:** `docs/memory.md` — единый источник; при конфликте с запросом — уточнять у пользователя; обновлять память в конце задачи при любом значимом изменении.
- **Что сделано:** каталог **ОТКК** — PostgreSQL (`otkk_cards`), UI `/planning/otkk`; просмотр «Открыть».
- **Что сделано:** **ОТКК в БД — структура карты** (пилот `otkk-1`): эталон в репо → seed в PostgreSQL при старте; **импорт .doc из UI удалён** (2026-06-16).
- **Что сделано:** **ОТКК-2** — эталон `otkk2_data.py` + `otkk2_segments.json` и сид в БД: п.4 = четыре кода СП через запятую; служебные поля Word `HYPERLINK "kodeks://..."` убираются (`sk_reporter/otkk_text.py`), не показывать в UI.
- **Что дальше:** ОТКК-2 — доводка **таблица за таблицей** вручную в `otkk2_segments.json` (табл. 1 — subrow-разбивка п.1; далее №2…); CSS в `app.css`.

---

## Быстрое состояние *(ранее «СЕЙЧАС В РАБОТЕ»)*

**Дата:** 2026-06-26  
**Главный фокус:** **планирование — расстановка / Прил.7** (согласование, без кода до закрытия открытых вопросов)  
**Документ:** [`docs/planning_deployment.md`](planning_deployment.md)  
**Параллельно:** предписания и `/daily` — не ломать  
**Статус планирования:** personnel, contractors (частично), projects, ОТКК — PostgreSQL; ждём Excel подрядчиков и проектов; таблица `positions` — запланирована  
**Референс:** `Noviy proekt AI/` — удалить после v1

**Новый чат:** начать с `docs/planning_deployment.md` → раздел «Открытые вопросы».

---

## Текущие решения

### Предписания (активно)

**Предписания:** без Техэксперт и без интернет-fallback. Нормативка — **локальная база** `data/normative/` (`manifest.yaml` + `texts/*.txt`). B18 перерабатывает Ollama; B19 **не меняется**, только сверка по фрагменту из базы.

**Поток предписаний:**

1. xlsx/xls → B18/B19 на листе «Форма заполнения предписания».
2. `normative_store.lookup_normative()` — поиск по manifest.
3. Ollama (`gemma4:31b-cloud`) — B18, сверка B19, отчёт.
4. В файл пишется только **B18**.

**Код:** `sk_reporter/prescriptions/normative_store.py`, `normative_parse.py`, `check_agent.py`.

**Не смешивать** с docx-агентами (`sk_reporter/agent/check_agent.py`); temp uploads — `UPLOAD_DIR`.

### Ollama
`sk_reporter/llm_client.py` — локально `http://127.0.0.1:11434` или облако: `OLLAMA_API_KEY` + `OLLAMA_HOST=https://ollama.com` (RelaxDev). Модель: `OLLAMA_MODEL` или `gemma4:31b-cloud`. TE env не нужен.

### Параллельно (стабильно, не ломать)
**Ежедневные отчёты СК** — `/reporting` → `/daily`: check→inject, руководитель, сборка в болванки. Одна кнопка **«Проверить и сформировать»**. Verify-агента **нет**.

**Отчёт инженера** — `/engineer-hub` → `/engineer/{profile_id}`: ВОР, ТК, docx (шаблон — вручную позже). **Разработка блока отложена.**

### Как работать агенту
1. Прочитать этот файл и [`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md).
2. **Предписания** — текущий приоритет; ежедневные отчёты и планирование — минимальный diff, не регрессить.
3. xlsm с макросами — **не перезаписывать** без `keep_vba`; экспорт только значений ячеек.
4. Коммит/push — только по просьбе пользователя.
5. Продакшен на RelaxDev — деплой из `main`, не bat на офисном ПК.

### ОТКК — перенос текста из Word (обязательно)
**Цель:** шесть пунктов карты **как видит пользователь в Word**, не «улучшать» и не пересказывать.

**Разрешено:** убрать только служебный мусор конвертации — поля `HYPERLINK "kodeks://..."`, подсказки `(утв. …)`, хвосты `Статус: Действующий документ…` (`sk_reporter/otkk_text.py`).

**Запрещено:** менять формулировки; подставлять `;` вместо `,`; выдумывать списки кодов; добавлять **СНиП** из tooltip ссылки (в п.4 ОТКК-2 только четыре **СП** через запятую).

**П.4 «Нормативные документы» (ОТКК-2):** `СП 126.13330.2017, СП 86.13330.2022, СП 70.13330.2012, СП 45.13330.2017`

**Код:** эталон `otkk{N}_data.py` + `otkk{N}_segments.json` в репо; в БД — `seed_otkk*` при старте (`otkk_db.py`). На RelaxDev после `git pull` — **перезапуск** сервера (автосид `seed_otkk2(overwrite=True)`).

---

## Очередь задач

### Сейчас
- Наполнить `data/normative/` (manifest + texts)
- Позже — UI загрузки НД в базу

### Отложено (не трогать без запроса)

#### Планирование — справочники → расстановка → Прил.7

Цепочка и этапы 0–7 **сделаны** (см. таблицы ниже). **Следующий код по планированию отложен:** доработка модуля проекты+назначения; полировка Прил.7 (см. «Доработки v1»).

#### Инженер ФИО — хаб, вкладки объектов, ВОР

**Сделано (2026-06-08, коммит `cf69962`, push в `main`):**

- `/engineer-hub` — карточки по **ФИО** инженеров с назначениями (PostgreSQL + `project.yaml`).
- URL карточки: `/engineer/{person_id}` из справочника; yaml-профили **удалены** (2026-06-16).
- `/engineer/{profile_id}` — карточка на RelaxDev.

**Отложено:** шаблон `data/engineer/report_template.docx` — **не автогенерировать**; положить вручную позже. Пока нет файла — предупреждение на странице инженера, кнопка docx неактивна.

**Код:** `sk_reporter/engineer/hub.py`, `webapp/static/engineer_hub.js`, правки `engineer.js`, `vor_data.py`, `project_store.py`.

---

### Справка: Планирование (архив решений, 2026-06-07)

**Цепочка продукта (актуально):**

```
personnel + project.yaml (назначения инженеров)
  → Расстановка (xlsm из справочника, раздел «Проекты»)
```

Ежедневный отчёт инженера: ФИО из PostgreSQL, проекты из `project.yaml` по `person_id`.

**Решение (2026-06-07):** правки **и в Excel, и на сайте**, с **синхронизацией** (yaml ↔ xlsx). Excel — для обмена и макросов; сайт — основное рабочее место.

**UI:** главная → **Планирование** / **Отчётность** / **Инженер ФИО**. Отчётность: `/reporting` → `/daily` и **`/prescriptions`**.

**ЛУВР (снят 2026-06-26):** модуль посещений, Прил.7 и расстановка из ЛУВР удалены из кода и репозитория. Исторические этапы 0–7 (yaml ↔ xlsx, appendix7, deployment) — не актуальны.

**Доработки v1 (отложено, точечно):** расстановка — подрядчик, водитель (кол. 8–9), режим из project.yaml.

**Принцип:** веб — основное рабочее место планирования; Excel — файлы обмена (xlsm).

**Не планировать «год вперёд» содержательно** — помесячно; в xlsx листы по месяцам (Январь…).

---

## Архитектура и пути

### 2026-06-08 — Предписания (реализовано, доводка TE)

- `/reporting` → `/prescriptions`: Excel xlsx/xls, проверка B18/B19, SSE, ZIP.
- Нормативка: TE → интернет-fallback → Ollama; B19 в файле = **краткий title источника** + пункты инженера (`f91ec0a`).
- Guard B18: сохранение объективных фактов; «Предложение модели» при откате.

### 2026-06-08 — Предписания (постановка, архив)

- Изначально: карточка «Предписания», загрузка и проверка **Excel** (не docx), UX как `/daily`.

### 2026-06-08 — Инженер ФИО (хаб + вкладки)

- Коммит `cf69962`: хаб карточек, `/engineer/{profile_id}`, автопрофиль при назначении.
- Шаблон docx — отложено, без автогенерации.

### 2026-06-07 — навигация и шапка
- Вложенные хабы: `/planning`, `/reporting`, `/engineer-hub`; хлебные крошки в `_header.html`
- *(устарело 2026-06-16)* офисный bat удалён — прод на RelaxDev

### 2026-06-04 — где лежат файлы (отчёты СК)
- **Сырые и исправленные:** temp `…/sk_reports_work/uploads/` (`UPLOAD_DIR`)
- **Готовые сводные:** temp `…/sk_reports_work/results/`
- После деплоя на RelaxDev — перезапуск сервиса

### 2026-06-05 — блок 2 без AI-агента
- `leader_switch.py` rule-based; verify **не планируется**

### 2026-06-04 — inject «Описание действий»
- Скачать исправленный — `/download/fixed/{имя}_исправлен.docx`

---

## Фон проекта (кратко)

- ~20 инженеров, ~40–45 ежедневных отчётов docx
- SK-Reporter: отчёты СК + планирование (PostgreSQL) + отчёт инженера по ВОР
- Детали продукта — `docs/PROJECT_CONTEXT.md`

---

## Не делать / осторожно

- Не возвращать `verify_agent`
- Не класть рабочие xlsx в **корень** репо — только `data/prescriptions/` или локально на диске
