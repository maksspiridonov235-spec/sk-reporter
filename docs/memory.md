# Память проекта (между чатами Cursor)

Краткие факты и решения. **Обновляй после важных договорённостей в чате.**

---

## ▶ СЕЙЧАС В РАБОТЕ (читать первым)

**Дата:** 2026-06-07  

### Главный фокус: **Планирование → ЛУВР**

ЛУВР — **от чего отталкиваемся** в планировании, не побочная вкладка.

**Цепочка продукта (целевая):**

```
ЛУВР (кто на объекте, по дням)
  → Персонал (person_id, справочник)
  → Объекты / проекты (закрепление людей)
  → Приложение 7 (отчёт о времени оказания услуг)
  → Расстановка
```

**Решение (2026-06-07):** правки **и в Excel, и на сайте**, с **синхронизацией** (yaml ↔ xlsx). Excel — для обмена и макросов; сайт — основное рабочее место.

**UI (актуально):** главная → **Планирование** → первой карточкой **ЛУВР** (`/planning/luvr`). Далее: Сотрудники, Проекты, ОТКК. Отдельно: **Отчётность** (`/daily`), **Инженер ФИО** (`/engineer-hub`).

| Этап | Содержание | Статус |
|------|------------|--------|
| 0 | Просмотр сводной таблицы по месяцам из `luvr.yaml` | ✅ есть |
| 1 | **Сетка как в Excel** (строки = люди, столбцы = дни) | 🔲 следующий код |
| 2 | **Редактирование ячеек** на сайте + сохранение в yaml | 🔲 |
| 3 | **Импорт из xlsx** / **экспорт в xlsx** (синхронизация) | 🔲 |
| 4 | Связь строк ЛУВР ↔ `person_id` (`personnel.yaml`) | 🔲 |
| 5 | Закрепление **объектов/проектов** за человеком в месяце | 🔲 |
| 6 | **Приложение 7** из шаблона xlsm + данных ЛУВР | 🔲 |
| 7 | **Расстановка** из шаблона xlsm | 🔲 |

**Не планировать «год вперёд» содержательно** — помесячно; в xlsx листы по месяцам (Январь…).

### Файлы `data/luvr/`

| Файл | В git | Назначение |
|------|-------|------------|
| `ЛУВР.xlsx` | нет | Источник посещений (офисный ПК) |
| `luvr.yaml` | **да** | Кэш для UI; `build_engineer_data.py --luvr` |
| `manifest.yaml` | **да** | Имена шаблонов Прил.7 и расстановки |
| `Прил 7 MOS.25.0008 … (макр).xlsm` | нет | Шаблон Приложения 7 (макросы) |
| `Отчёт-расстановка ООО ТЕХРЕСУРС макр.xlsm` | нет | Шаблон расстановки (макросы) |

Шаблоны **перенесены из корня репо** в `data/luvr/` (2026-06-07). На офисном ПК после `git pull` — положить xlsx/xlsm в ту же папку (или скопировать с Mac).

**Код:** `sk_reporter/luvr_store.py`, UI — `webapp/static/luvr.js`, `planning.js` (раздел luvr), API — `/api/luvr`, `/api/planning/luvr`.

**Фикс (2026-06-07, не в push):** UI показывает таблицу из `luvr.yaml` **без** xlsx на диске (раньше требовал xlsx — на Windows было «пусто»).

---

### Параллельно (стабильно, не ломать)

**Ежедневные отчёты СК** — `/reporting` → `/daily`: check→inject, руководитель, сборка в болванки. Одна кнопка **«Проверить и сформировать»**. Verify-агента **нет**.

**Отчёт инженера** — `/engineer-hub` → `/engineer`: ВОР, ТК, docx.

Новый чат по **ЛУВР** — начинать с таблицы этапов выше и `data/luvr/README.md`.

### Как работать агенту

1. Прочитать этот раздел и [`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md).
2. ЛУВР — приоритет; ежедневные отчёты — минимальный diff, не регрессить.
3. xlsm с макросами — **не перезаписывать** без `keep_vba`; экспорт только значений ячеек.
4. Коммит/push — только по просьбе пользователя.
5. После `git pull` на офисном ПК — перезапуск bat, `pip install -e .` при новых зависимостях, Ctrl+F5.

---

## Архитектура и пути

### 2026-06-07 — навигация и шапка
- Вложенные хабы: `/planning`, `/reporting`, `/engineer-hub`; хлебные крошки в `_header.html`
- `launcher/SK-Reporter.bat` — явный pip install, проверка openpyxl/pyyaml

### 2026-06-04 — где лежат файлы (отчёты СК)
- **Сырые и исправленные:** temp `…/sk_reports_work/uploads/` (`UPLOAD_DIR`)
- **Готовые сводные:** temp `…/sk_reports_work/results/`
- После `git pull` на офисном ПК — **перезапустить** bat

### 2026-06-05 — блок 2 без AI-агента
- `leader_switch.py` rule-based; verify **не планируется**

### 2026-06-04 — inject «Описание действий»
- Скачать исправленный — `/download/fixed/{имя}_исправлен.docx`

---

## Фон проекта (кратко)

- ~20 инженеров, ~40–45 ежедневных отчётов docx
- SK-Reporter: отчёты СК + **планирование по ЛУВР** + отчёт инженера по ВОР
- Детали продукта — `docs/PROJECT_CONTEXT.md`

---

## Не делать / осторожно

- Не возвращать `verify_agent`
- Не коммитить `data/luvr/*.xlsx`, `*.xlsm` (макросы, локальные данные)
- Не класть рабочие xlsx в **корень** репо — только `data/luvr/`
